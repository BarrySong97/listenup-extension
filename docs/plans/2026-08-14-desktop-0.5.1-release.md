# ListenUp Desktop 0.5.1 浏览器字幕重接修复发布

- 日期：2026-08-14
- 状态：完成
- Plane：LISTENUP-18
- 修复：LISTENUP-17

## 发布目标

发布 Desktop 退出自播后，浏览器刷新可重新激活字幕来源的修复。刷新产生的新 session 即使尚未
播放，也应恢复字幕快照；同一 bridge + tab 的旧 session 不得残留为幽灵播放候选。

## 发布范围

- Desktop 版本从 0.5.0 升到 0.5.1；Extension 与 Website 版本不变。
- 同步 `package.json`、Tauri 配置、Cargo manifest / lockfile 与 Desktop 发布基线文档。
- 推送 annotated tag `v0.5.1`，由 `release-desktop.yml` 构建 Apple Silicon DMG、updater
  `.app.tar.gz`、minisign 签名和 `latest.json`，全部成功后自动公开 Release。
- Native Messaging 协议版本与 production / DEV 环境标识不变。

## 执行清单

1. [x] 浏览器刷新重接修复完成并写入 LISTENUP-17。
2. [x] 同步 0.5.1 的全部版本来源并确认 production / development 标识隔离。
3. [x] 运行 Desktop tests/build、Rust tests、环境标识 sensor、docs sensor 与 diff 检查。
4. [x] 提交版本变更，推送当前发布分支并创建、推送 annotated tag `v0.5.1`。
5. [x] 等待 GitHub Actions release workflow 成功且 Release 从 draft 转为公开。
6. [x] 验证 Release 资产、`latest.json` 版本/公开下载 URL、官网 Latest 链路与 updater 可发现性。
7. [x] 把发布链接、workflow 和验证证据回写本记录与 Plane，再将 LISTENUP-18 更新为完成。

## 发布前验证记录

- Desktop 前端 tests：30/30 通过；TypeScript / Vite production build 通过。
- Rust tests：47/47 通过；`cargo check` 与 `cargo fmt --check` 通过。
- `node scripts/check-environment-identifiers.mjs`、`node scripts/check-docs.mjs` 与
  `git diff --check`：通过。
- `package.json`、Tauri config、Cargo manifest / lockfile 均为 `0.5.1`；production / DEV
  标识保持隔离。
- 仓库 `target` 下没有 `bundle/macos/ListenUp*.app` 残留；正式资产只认 tag CI。

## 发布后验证记录

- 修复提交：`8034c30 fix(desktop): reconnect browser subtitles after refresh`；版本提交：
  `9f099a0 chore(desktop): release 0.5.1`。annotated tag `v0.5.1` 的 tag object 为 `a2712e92`，
  精确指向版本提交。
- GitHub Actions run `31802575954` 全部成功，耗时 11 分 27 秒；Build draft release、DMG
  公证/替换、updater URL 重写和 Publish release 均通过：
  <https://github.com/BarrySong97/listenup-extension/actions/runs/31802575954>。
- Release `ListenUp Desktop v0.5.1` 于 2026-08-14 13:08:26 UTC 公开，`isDraft=false`、
  `isPrerelease=false`，目标提交为 `9f099a0`：
  <https://github.com/BarrySong97/listenup-extension/releases/tag/v0.5.1>。
- 公开资产共四项：`latest.json`、Apple Silicon DMG、updater `.app.tar.gz` 与 `.sig`。DMG
  SHA-256 为 `59fb74e7c2ca20d2d8312a36041649e0516800594fd92fc26469d50b0d04dd0e`，updater
  SHA-256 为 `c48f851237e3400ceea5c21e1dcd030188438ec416e473a51fd39fc80a2b3cd0`。
- 公开 `latest.json` SHA-256 为 `c79c27b5cf47cdc6278a71b84158fa137bbdb01e4495523c1f00c6549bd9d7c9`，
  版本为 `0.5.1`；`darwin-aarch64` 与 `darwin-aarch64-app` 都含非空签名，URL 指向公开
  `releases/download/v0.5.1/ListenUp.Desktop_0.5.1_aarch64.app.tar.gz`，不使用 assets API。
- `releases/latest` 返回 302 并精确解析到 `/releases/tag/v0.5.1`；Release 页面与 updater 包
  最终均返回 HTTP 200。
- Workflow 仍报告 GitHub 官方 Action Node.js 20 runtime 弃用警告；不影响本次成功发布，继续由
  既有技术债 LISTENUP-9 跟踪 action major 升级。

## 风险与回归

- 正式构建必须来自干净 tag；workflow 任一步失败应保持 draft，不手工公开不完整资产。
- `latest.json` 必须指向 `github.com/.../releases/download/v0.5.1/`，不能使用 GitHub assets API。
- 发布资产必须包含 DMG、updater `.app.tar.gz`、签名和 `latest.json`。
- 本地不构建 bundle；正式资产只认 tag CI，仓库 `target` 下不得残留可被 LaunchServices 登记的 app。
