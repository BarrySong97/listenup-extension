# Extension 1.5.2 与 Desktop 0.3.3 发布记录

- 日期：2026-08-07
- 状态：执行中
- 发布顺序：Chrome Extension → ListenUp Desktop

## Chrome Extension 1.5.2

- 版本提交：`ed8abcf`（`chore(extension): release 1.5.2`）。
- 商店包：`listenup-extension-1.5.2-ed8abcf-chrome.zip`，zip 根目录直接包含
  `manifest.json`；SHA-256 为
  `14d9d680c300df74a7e1945e980842654e49d7373e5ec971103c7cdcd4c8ce32`。
- GitHub Actions 构建 `31166732446` 已通过；同时将失效的
  `actions/upload-artifact@v3` 升级到 v4，并把 CI Node 基线从 18.17.1 升级到 22.17.0。
- Chrome Web Store 已接受草稿 `1.5.2` 并进入待审核；提交时已关闭“通过审核后自动发布”，
  因此线上继续保留 `1.5.1`，待 Desktop 0.3.3 发布并验证后再手动发布扩展。

## ListenUp Desktop 0.3.3

- 版本来源同步更新：`package.json`、`tauri.conf.json`、`Cargo.toml` 与本 crate 的
  `Cargo.lock` 条目。
- 发布前验证：Desktop 前端测试与构建、Rust 测试、环境标识 sensor、文档 sensor 和 diff 检查。
- 验证通过后推送 annotated tag `v0.3.3`，由 Desktop release workflow 构建、签名、公证、
  staple、生成 updater 资产并自动发布 GitHub Release。

## 最终验收

- [x] Extension 1.5.2 干净 CI 构建通过。
- [x] Extension 1.5.2 上传并以 deferred publishing 方式提交审核。
- [x] Desktop 0.3.3 发布前验证通过：11 项前端测试、生产前端构建与 25 项 Rust 测试通过。
- [ ] `v0.3.3` workflow 成功且 GitHub Release 已公开。
- [ ] `latest.json` 指向公开的 `releases/download/v0.3.3/` 更新资产。
