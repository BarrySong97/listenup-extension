# ListenUp Desktop 0.5.4 字幕工具行尺寸修复发布

- 日期：2026-08-15
- 状态：已完成
- Plane：LISTENUP-29
- 修复：LISTENUP-28

## 发布目标

发布用户已在 Desktop DEV 验收的目标语言选择器尺寸修复：语言 Select 与 Source / Translation /
Bilingual 模式 Tab 共享 24px 高度、6px 圆角和 10px 文字，显式重置 HeroUI 默认 min-height、
padding 与内部 Value 字号；Native Messaging、字幕数据和 updater 链路不变。

## 发布范围

- Desktop 版本从 0.5.3 升到 0.5.4；Extension 与 Website 版本不变。
- 同步 `package.json`、Tauri 配置、Cargo manifest / lockfile。
- 推送 annotated tag `v0.5.4`，由 `release-desktop.yml` 构建、签名、公证并公开 DMG、updater
  `.app.tar.gz`、签名与 `latest.json`。
- GitHub Release 说明记录字幕语言选择器与模式 Tab 的视觉对齐修复。

## 执行清单

1. [x] LISTENUP-28 已完成修复、UI 棘轮、Desktop DEV 验收与独立提交。
2. [x] 创建 LISTENUP-29，并将发布事项推进到 In Progress。
3. [x] 同步 0.5.4 的全部版本来源与发布基线文档。
4. [x] 运行 Desktop tests/build、Rust tests、环境/docs sensors 与 diff 检查。
5. [x] 提交版本变更，推送发布分支与 annotated tag `v0.5.4`。
6. [x] 等待 workflow 完成正式构建、签名、公证、DMG 替换、updater URL 重写与公开 Release。
7. [x] 验证四项资产、哈希、`latest.json` 版本/签名/公开 URL、Gatekeeper、公证与 Latest 链路。
8. [x] 回写发布记录与 Plane，将 LISTENUP-29 更新为完成。

## 发布前验证

- `pnpm --filter @listenup/desktop test`：35/35 通过，包含模式 Tab 与目标语言 Select 共用紧凑
  尺寸、HeroUI 内部 Value 重置的 UI 棘轮。
- `pnpm --filter @listenup/desktop build`：TypeScript 与 Vite production build 成功。
- `cargo test --manifest-path apps/listenup-desktop/src-tauri/Cargo.toml`：52/52 通过。
- `node scripts/check-environment-identifiers.mjs`、`node scripts/check-docs.mjs` 与
  `git diff --check`：通过。

## 正式发布验证

- 修复提交：`fa68c837d914737f63183db7b38c116549b65f55`；发布提交：
  `a40f856c48fc5286ac7782c182d05dd55b05014a`；annotated tag：`v0.5.4`。
- GitHub Actions [run 31871717202](https://github.com/BarrySong97/listenup-extension/actions/runs/31871717202)
  用时 9m40s，正式构建、Developer ID 签名、应用与 DMG 公证、DMG 替换、updater URL 重写和
  公开发布全部成功。
- 公开 Release：<https://github.com/BarrySong97/listenup-extension/releases/tag/v0.5.4>，为非 draft、
  非 prerelease，并已进入 `releases/latest`；target commit 为发布提交。
- Release 包含且仅包含 `latest.json`、aarch64 `.app.tar.gz`、对应 `.sig` 和 aarch64 `.dmg`
  四项必需资产；`latest.json` 版本为 `0.5.4`，两项 darwin updater URL 均指向公开 Release
  下载地址、无 `api.github.com`，内嵌签名与独立 `.sig` 文件一致。
- 下载的 DMG 与内部 `.app` 均通过 Gatekeeper，来源为 `Notarized Developer ID`；二者的 stapler
  ticket 均有效，`.app` 通过 `codesign --verify --deep --strict`。Bundle identifier 为
  `com.listenup.desktop`，short/build version 均为 `0.5.4`，主程序与 CLI sidecar 均为 arm64。
- SHA-256：`latest.json` 为 `9780b0aa2b66df63f46dccdcb92790f56d28b13aa159df90eb65e6d7fe942ce9`；
  `.app.tar.gz` 为 `a36e3f7529c8ad734ca7a4395814bccae2785da39fead7fab836bf3a3939a94e`；
  `.sig` 为 `7a66e419dc66f93e54878ef62976bbfd3d52fa00edb0ce6541055c2538a02786`；
  `.dmg` 为 `ce1db0712dc716b1aa3e9f62e87c76cd6c1af9035c72077aeddf033279cab9ab`。
- Release 正文由 workflow 留空；当前本机 `gh release edit` 身份对编辑端点返回 404，因此未在
  发布后补写正文，不影响公开资产、官网 latest 链路或应用内更新。
- workflow 的 Node 20 action deprecation annotation 不影响本次产物；升级事项继续由
  LISTENUP-27 跟踪。

## 风险与回归

- 本次只改变字幕工具行的 CSS 尺寸，不得改变字幕模式、目标语言选择或播放控制状态。
- 340px 最小宽度下语言 Select 仍需可读、可点击，且不能挤压播放按钮。
- workflow 任一步失败保持 draft，不手工公开不完整资产；正式资产只认 tag CI。
